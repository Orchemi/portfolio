'use client';

import classNames from 'classnames/bind';
import style from './MidasMenuEdit.module.scss';

const cx = classNames.bind(style);

export default function MidasMenuEdit() {
  return <div className={cx('home')}>Midas Menu Edit</div>;
}
